
<div class="container">
  <%= simple_form_for([@cocktail, @dose]) do |f| %>
    # <%= f.input :ingredient, collection: Ingredient.all %>
    <%= f.input :description %>
    <%= f.submit "ajouter les doses" %>
  <% end %>
</div>
